[package]
name = "tvm-common"
version = "0.1.0"
authors = ["TVM Contributors"]
license = "Apache-2.0"

[features]
bindings = []

[dependencies]
error-chain = { version = "0.12.0", default-features = false }

[build-dependencies]
bindgen = "0.37.4"
